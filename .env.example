PORT=4177

# Optional. Without this, users can paste their own eBird token in the app.
EBIRD_API_KEY=

# Optional. OpenStreetMap mode works without map keys.
# MAP_PROVIDER=osm
# MAP_PROVIDER=google
# GOOGLE_MAPS_BROWSER_KEY=
# GOOGLE_MAPS_SERVER_KEY=
